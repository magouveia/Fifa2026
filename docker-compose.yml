version: '3.8'

services:
  mundial-2026-app:
    build: .
    container_name: mundial-2026-app
    restart: unless-stopped
    ports:
      - "5173:5173"
    environment:
      - NODE_ENV=production
      - DISABLE_AUTO_OPEN=true
      - JWT_SECRET=your_super_secret_jwt_key_here
      - PORT=5173
      - DB_PATH=data/mundial.db
    volumes:
      - ./data:/app/data
